# === Database (MySQL) ===
DB_URL=jdbc:mysql://localhost:3306/bookstore?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
DB_USERNAME=root
DB_PASSWORD=root

# === JWT ===
JWT_ISSUER=bookstore
# 최소 32자 이상
JWT_ACCESS_SECRET=CHANGE_ME_ACCESS_SECRET_32CHARS_MIN________
JWT_ACCESS_EXP_MINUTES=15

JWT_REFRESH_PEPPER=CHANGE_ME_REFRESH_PEPPER________
JWT_REFRESH_EXP_DAYS=14

# === CORS ===
# 콤마(,)로 여러 Origin 허용 가능
APP_CORS_ALLOWED_ORIGINS=http://localhost:3000
